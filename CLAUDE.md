# پلتفرم بینا — راهنمای عامل

نسخه‌ی فارسی و تحت وب Chat2DB Community.

## طراحی رابط کاربری

برای **هر** کار طراحی فرانت‌اند در این مخزن از مهارت `impeccable` استفاده کن:
بازطراحی، نقد، پولیش، رنگ، تایپوگرافی، چیدمان، انیمیشن، حالت‌های خالی و خطا،
دسترس‌پذیری، و توکن‌های طراحی.

```
/impeccable init      # یک بار در هر پروژه
```

مهارت در `.agents/skills/impeccable/` است و از `.claude/skills/` به آن symlink شده،
پس با کلون‌کردن مخزن در دسترس است و نیازی به نصب دوباره ندارد.

مهارت با `https://impeccable.style/api` تماس می‌گیرد تا کاتالوگ طراحی بگیرد.
برای خاموش‌کردن این ارتباط:

```bash
export DO_NOT_TRACK=1
```

## نکات این مخزن که وقت تلف می‌کنند

**زبان و چیدمان.** فارسی زبان پیش‌فرض است ولی چیدمان در همه‌ی زبان‌ها LTR می‌ماند و
این عمدی است — ویرایشگر SQL، جدول نتایج و درخت اشیاء ذاتاً محتوای چپ‌به‌راست دارند.
`ConfigProvider` این را صریحاً تثبیت می‌کند؛ آن را به RTL برنگردان مگر اینکه کاربر
صریحاً بخواهد.

**رنگ‌ها.** پالت در `src/constants/palette.ts` است. اکسنت فقط وقتی اعمال می‌شود که
کاربر رنگی در تنظیمات انتخاب نکرده باشد، تا انتخابگر رنگ نشکند.

**تنظیمات کاربر ماندگار است.** تغییر پیش‌فرض‌ها در `DEFAULT_BASE_SETTINGS` روی نصب
موجود اثر ندارد؛ `localStorage` بر آن اولویت دارد. موقع تست، یا `localStorage` را
پاک کن یا از پنجره‌ی ناشناس استفاده کن.

**فونت‌ها و CORS.** فایل‌های فونت همیشه با هدر `Origin` درخواست می‌شوند، برخلاف
XHR معمولی. اگر فونت‌ها ۴۰۳ گرفتند، مشکل از `CorsFilter` است نه از CSS —
`CHAT2DB_COMMUNITY_ALLOWED_ORIGINS` را ببین.

**تست‌ها پیش‌فرض اجرا نمی‌شوند.** `maven.test.skip=true` در BOM تنظیم شده؛ برای
اجرا `-Dmaven.test.skip=false` بده.

**اعتبارسنجی i18n.** بعد از هر تغییر در `src/i18n/` این را اجرا کن:

```bash
cd chat2db-community-client && yarn test:i18n
```

کلیدها را هرگز ترجمه نکن — فقط مقادیر. کلید یک شناسه است که کد با آن `i18n()` صدا می‌زند.

## نقشه‌ی معماری

`graphify-out/` یک گراف دانش از مخزن دارد. برای پرسش‌های معماری به‌جای grep زدن
روی هزاران فایل، از آن استفاده کن:

```bash
gunzip -k graphify-out/graph.json.gz   # یک بار بعد از کلون
graphify query "<سؤال>"
```

جزئیات و محدودیت‌ها در `graphify-out/README.md`.
